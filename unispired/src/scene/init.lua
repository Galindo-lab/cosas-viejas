init = {
    load = function()
       -- player.drawable = graphics.newImage("images/player.png")
    end,

    draw = function()
       --
    end,

    update = function(dt)
       Scene.play(game_scene)
    end,
}
