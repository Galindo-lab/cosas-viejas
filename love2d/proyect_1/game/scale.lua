
config = {
   gameWidth    = 0,
   gameHeight   = 0,
   windowWidth  = 0,
   windowHeight = 0,
   
   config = {
      fullscreen = true,
   }
}


push:setupScreen(gameWidth, gameHeight, windowWidth, windowHeight, {fullscreen = true})

push:setupScreen()
